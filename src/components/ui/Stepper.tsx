import { Check } from 'lucide-react'
interface StepperProps { steps: string[]; activeStep: number }
export function Stepper({ steps, activeStep }: StepperProps) { return <ol className="grid grid-cols-3 gap-2" aria-label="Upload progress">{steps.map((step, index) => { const number = index + 1; const complete = number < activeStep; return <li key={step} className="flex min-w-0 items-center gap-2"><span className={`grid size-6 shrink-0 place-items-center rounded-full text-xs font-semibold ${complete ? 'bg-[#D97757] text-white' : number === activeStep ? 'border-2 border-[#D97757] text-[#D97757]' : 'bg-stone-100 text-stone-400'}`}>{complete ? <Check className="size-3.5" /> : number}</span><span className={`hidden truncate text-xs font-medium sm:block ${number === activeStep ? 'text-stone-900' : 'text-stone-400'}`}>{step}</span></li> })}</ol> }
