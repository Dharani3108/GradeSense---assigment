import { CheckCircle2, FileText, UploadCloud } from 'lucide-react'
import type { UploadFile } from '../../types/grading'
import { Button } from '../ui/Button'
import { Card } from '../ui/Card'
interface UploadCardProps { label: string; hint: string; file?: UploadFile; onUpload: () => void }
export function UploadCard({ label, hint, file, onUpload }: UploadCardProps) { return <Card className="p-3 sm:p-4">{file ? <div className="flex items-center gap-3 rounded-2xl bg-green-50 p-4"><CheckCircle2 className="size-5 text-green-600" /><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium">{file.name}</p><p className="mt-0.5 text-xs text-stone-500">{file.size} · Ready for review</p></div><Button variant="secondary" size="sm" onClick={onUpload}>Replace</Button></div> : <button type="button" onClick={onUpload} className="flex min-h-56 w-full flex-col items-center justify-center rounded-2xl border border-dashed border-stone-300 bg-[#FCFBFA] p-6 text-center transition hover:border-[#D97757] hover:bg-[#fdf7f4]"><span className="mb-4 grid size-12 place-items-center rounded-2xl bg-white shadow-sm"><UploadCloud className="size-5 text-[#D97757]" /></span><p className="text-sm font-semibold">Upload {label}</p><p className="mt-2 max-w-xs text-xs leading-5 text-stone-500">{hint}</p><span className="mt-4 inline-flex items-center gap-2 text-xs font-medium text-[#c56649]"><FileText className="size-3.5" />Choose a file</span></button>}</Card> }
