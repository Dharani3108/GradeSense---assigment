node_modules/
dist/
.env
# The whole directory: WAL mode also creates .sqlite-wal and .sqlite-shm,
# which hold live grading data.
data/
uploads/question-papers/*
uploads/rubrics/*
uploads/student-answers/*
uploads/reports/*
!uploads/**/.gitkeep
